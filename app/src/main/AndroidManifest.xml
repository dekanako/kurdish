<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="salahadin.software.com.kurdish">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/title_book_id"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChalakiActivity" />
        <activity android:name=".RahenanActivity" />
        <activity android:name=".SpecialBabatActivity" />
        <activity android:name=".BabatActivity" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".WshawWataActivty" />
        <activity android:name=".VidioPlayingActivity" />
        <activity android:name=".VideoListAtivity" />
    </application>

</manifest>